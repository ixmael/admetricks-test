[tool.poetry]
name = "backend"
version = "0.1.0"
description = "The backend for admetricks test"
authors = ["ixmael <hola@irm.mx>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "^2.3.3"
flask-restful = "^0.3.10"
flask-cors = "^4.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
